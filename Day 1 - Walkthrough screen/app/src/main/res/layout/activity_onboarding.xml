<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/white">

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/onboard_viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>


        <Button
            android:id="@+id/btstarted"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_40sdp"
            android:layout_marginHorizontal="@dimen/_85sdp"
            android:backgroundTint="#74AA80"
            android:text="get started"
            android:textSize="14sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="@dimen/_25sdp"/>

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabs"
            android:layout_width="@dimen/_20sdp"
            android:layout_height="@dimen/_6sdp"
            android:background="@null"
            app:tabBackground="@drawable/tab_selector"
            app:layout_constraintStart_toStartOf="@id/btstarted"
            app:layout_constraintEnd_toEndOf="@id/btstarted"
            app:layout_constraintBottom_toTopOf="@id/btstarted"
            android:layout_marginBottom="@dimen/_25sdp"
            app:tabIndicatorHeight="0dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>